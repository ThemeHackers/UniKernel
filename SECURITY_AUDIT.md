# รายงานตรวจสอบความปลอดภัย: KernelUNO OS Core

วันที่: 2026-05-01  
ขอบเขต: `KernelUNO.ino`  
หัวข้อที่เน้น: Buffer Overflow และ Privilege Escalation

## ประเด็นความเสี่ยงระดับสูง

1. **การเขียนหน่วยความจำเกินขอบเขตในพาร์สคำสั่ง `gpio` (Buffer Overflow)**
   - ตำแหน่ง: `executeCommand()` ในสาขาคำสั่ง `gpio`
   - สาเหตุหลัก: ค่า `sp = indexOf(args, " ")` ไม่ได้ตรวจขอบเขตก่อนนำไปใช้เป็น index ของ `char pinStr[8]`
   - ผลกระทบ: หาก token ตัวแรกของคำสั่ง `gpio` ยาวผิดปกติ จะทำให้ `pinStr[sp] = '\0'` เขียนเกินบัฟเฟอร์บนสแตก และอาจทำให้หน่วยความจำข้างเคียงเสียหาย
   - ความเป็นไปได้ในการโจมตี: สูง (เข้าถึงได้ผ่าน serial และ telnet เมื่อผ่านการยืนยันตัวตนแล้ว)

2. **การยึดรหัสผ่าน root ได้ในช่วงตั้งค่าเริ่มต้น (Privilege Escalation / Insecure Bootstrap)**
   - ตำแหน่ง: เงื่อนไขคัดกรองคำสั่งใน `loop()` และตัวจัดการคำสั่ง `passwd`
   - สาเหตุหลัก: เมื่อ `needsSetup` เป็นจริง ผู้ใช้ที่ยังไม่ล็อกอินยังสั่ง `passwd` ได้ โดยโค้ดแนะนำว่าให้ทำผ่าน serial เท่านั้น แต่ไม่ได้บังคับจริงในทางเทคนิค
   - ผลกระทบ: ผู้โจมตีที่เข้าถึง telnet ระยะไกลอาจตั้งรหัสผ่าน root ก่อนเจ้าของเครื่องและยึดระบบได้ หากอุปกรณ์ยังตั้งค่าไม่เสร็จ
   - ความเป็นไปได้ในการโจมตี: สูง โดยเฉพาะอุปกรณ์ ESP8266 ที่เปิดช่องทาง telnet

## ประเด็นความเสี่ยงระดับกลาง

3. **`hashPass` อาจไม่ปิดสตริงด้วย `\0` เสมอ (Memory Safety + ความไม่เสถียรของการยืนยันตัวตน)**
   - ตำแหน่ง: `hashPass(const char* input, char* output)`
   - สาเหตุหลัก: ลูปไม่การันตีการปิดท้าย `\0` เมื่ออินพุตยาวตั้งแต่ 9 ตัวอักษรขึ้นไป
   - ผลกระทบ: `strcmp` อาจอ่านข้อมูลเกินขอบเขต ทำให้พฤติกรรมยืนยันตัวตนคาดเดาได้ยาก

4. **ไม่มีการแยกสิทธิ์คำสั่งสำคัญ (Privilege Model อ่อนแอ)**
   - ตำแหน่ง: ตัวกระจายคำสั่งทั้งหมด
   - สาเหตุหลัก: ใช้เพียงแฟลก `authenticated` เดียว และทุก session ถูกมองเป็น `root`
   - ผลกระทบ: เมื่อใดก็ตามที่ผ่านการยืนยันตัวตนได้ จะสั่งคำสั่งสำคัญ/ทำลายระบบได้ทั้งหมดทันที

## หลักการโจมตี (เชิงแนวคิดเพื่อการป้องกัน)

> ส่วนนี้อธิบายเพื่อช่วยออกแบบการป้องกัน ไม่ให้รายละเอียดเชิงปฏิบัติที่นำไปใช้โจมตีโดยตรง

### A) การโจมตีผ่านโค้ด (Code-path Abuse)
- **Memory Corruption Primitive**: ใช้อินพุตยาวเกินขนาดบัฟเฟอร์คงที่ เพื่อสร้างการเขียนทับหน่วยความจำข้างเคียง
- **Parser Trust Abuse**: อาศัยจุดพาร์สที่เชื่ออินพุตมากเกินไป (เช่นใช้ index โดยไม่ตรวจขอบเขต)
- **Authentication-state Confusion**: ทำให้สถานะการยืนยันตัวตนสับสน เช่นการเทียบสตริงจากข้อมูลที่ไม่ถูกปิดท้ายอย่างถูกต้อง

### B) การโจมตีผ่านการใช้งานจริง (Operational Abuse)
- **Insecure Bootstrap Race**: ช่วงที่ระบบยังไม่ตั้งรหัสผ่านเป็นช่วงเสี่ยงสูง หากเปิด remote management ก่อนล็อกขั้นตอน onboarding
- **Privilege Concentration**: เมื่อสิทธิ์สำคัญรวมศูนย์ที่ root role เดียว ความเสียหายหลังผ่านด่าน auth จะเป็น full compromise ทันที
- **Management-plane Exposure**: การเปิดบริการบริหารระบบบนเครือข่ายใช้งานทั่วไปเพิ่มพื้นผิวโจมตีอย่างมาก

## สิ่งที่โค้ดทำได้ดีอยู่แล้ว

- จำกัด `inputBuffer` ด้วยเงื่อนไข `inputLen < 63` ก่อนเขียน และมีการปิดสตริงก่อนนำไปประมวลผล
- หลายตำแหน่งใช้การคัดลอกแบบกำหนดขนาดพร้อมบังคับใส่ `\0` ท้ายบัฟเฟอร์
- มีการจำกัดความลึก recursion (`MAX_SHELL_DEPTH`) ลดความเสี่ยงจากสคริปต์เรียกซ้อน

## ข้อเสนอแนะการแก้ไข (เรียงตามลำดับความสำคัญ)

1. **แก้พาร์ส `gpio` ให้ปลอดภัย**
   - ตรวจความยาว token ให้ชัดเจน (`sp <= 0 || sp >= sizeof(pinStr)` ให้ปฏิเสธทันที)
   - ใช้ bounded copy และใส่ตัวปิดสตริงอย่างชัดเจน

2. **บังคับให้ตั้งรหัสผ่านช่วงเริ่มต้นได้จากช่องทาง local เท่านั้น**
   - อนุญาต `passwd` ตอน setup เฉพาะ trusted local channel
   - ปิด telnet จนกว่าจะพ้นสถานะ `needsSetup == true`

3. **ทำให้กระบวนการ hash/เก็บรหัสผ่านแข็งแรงขึ้น**
   - การันตีการปิดสตริงทุกกรณี
   - เปลี่ยนจากอัลกอริทึมแปลงแบบกำหนดเองไปใช้แนวทาง hash รหัสผ่านที่เหมาะกับข้อจำกัดของ MCU

4. **ออกแบบสิทธิ์แบบ Least Privilege**
   - แยกคำสั่งอ่านข้อมูลออกจากคำสั่งระดับผู้ดูแล
   - เพิ่มการยืนยันตัวตนซ้ำสำหรับคำสั่งที่มีผลกระทบสูง
